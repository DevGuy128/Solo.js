var express = require('express');
var app =express();
var db = function(name,rows,columns){
  
};
var createDB = function(dbName){
   
};
var start = function(){
  app.listen(3000);
  app.on('disconnect')
};
